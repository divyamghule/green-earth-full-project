# Optional Custom Model (Future Upgrade)

This folder is kept for future model integration.

Current project runtime uses a TensorFlow.js heuristic classifier in:
- backend/services/ai.service.js

You can later replace it with a trained model pipeline.
